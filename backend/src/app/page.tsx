// Placeholder root route. This project is the API only — see src/app/api/
// for endpoints. A separate frontend project should consume this API.

export default function StatusPage() {
  return <p>PantryMatch API is running. Endpoints live under /api.</p>;
}
