# Where the backend API is running
NEXT_PUBLIC_API_URL=http://localhost:4000

# Supabase (used directly by the frontend for auth — sign up/login/session)
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
