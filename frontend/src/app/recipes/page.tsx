// Browse recipes ranked by match % against the user's pantry.
// Data source: GET /api/recipes
// TODO: render list of <RecipeCard /> with <MatchBadge /> (src/components/recipes/)

export default function RecipesPage() {
  return (
    <main className="mx-auto max-w-5xl px-4 py-10">
      <h1 className="text-2xl font-semibold">Recipes You Can Make</h1>
    </main>
  );
}
