"use client";

// Autocomplete search (GET /api/ingredients/search?q=) + add-to-pantry form.
// TODO: debounced search input, quantity/unit/storage fields, POST /api/pantry/items

export function ManualAddSearch() {
  return <div>{/* TODO */}</div>;
}
